apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config
  namespace: mysql
data:
  init.sql: |
    GRANT ALL PRIVILEGES ON todolist.* TO 'todoapp'@'%';
    USE todolist;
    CREATE TABLE counter (
      id INT AUTO_INCREMENT PRIMARY KEY,
      value INT
    );